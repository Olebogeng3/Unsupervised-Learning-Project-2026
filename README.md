# Unsupervised-Learning-Project-2026

## Table of Contents

- **`data/`**: raw and processed datasets used for experiments.
- **`notebooks/`**: quickstart and analysis notebooks (e.g. `notebooks/unsupervised_quickstart.ipynb`).
- **`Mock_student_packet_v4/`**: original project materials and student notebooks.
- **`models/`**: saved model artifacts (e.g. `models/pca.pkl`, `models/kmeans_pipeline.pkl`).
- **`results/`**: evaluation outputs and presentation files.
- **`figures/`**: generated plots and visualizations used in reports.
- **`scripts/`**: helper scripts for building presentations and uploads.
- **`requirements.txt`**: Python dependencies for the project environment.

## Quick Start

1. Install dependencies:

```bash
pip install -r requirements.txt
```

2. Open notebooks in `notebooks/` or `Mock_student_packet_v4/` to explore analyses.

3. Models and evaluation outputs are in `models/` and `results/`.

---

*(This Table of Contents was generated automatically.)*
# Unsupervised-Learning-Project-2026